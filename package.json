{
  "name": "goal-project",
  "version": "1.0.0",
  "description": "Personal Goal Project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server-dev": "npm run dev --prefix backend",
    "client-dev": "npm run dev --prefix frontend",
    "client-build": "npm run build --prefix frontend"
  },
  "author": "",
  "license": "ISC"
}
